listen_addr: 127.0.0.1:8080
callback_token: test-token
state_db: /var/lib/sre-rca/state.db
aliyun:
  profile: sre-ecs-role
  workspace: default-cms-demo
  region: cn-hangzhou
worker:
  max_turns: 8
  timeout_seconds: 300
